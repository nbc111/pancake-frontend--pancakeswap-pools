import React from 'react'
import { Loading } from '@pancakeswap/uikit'

/**
 * LocalLoader - Simple loading component
 * Replaces deleted views/V3Info/components/Loader
 */
export const LocalLoader: React.FC = () => {
  return <Loading />
}
